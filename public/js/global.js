// 'use strict';
// document.addEventListener('DOMContentLoaded', function () {
//     let $scrollPos = window.scrollY;
//     const $header = document.getElementById('header-top-block');
//     const $headerHeight = $header.offsetHeight;
//     const $addClassOnScroll = () => $header.classList.add('translucent');
//     const $removeClassOnScroll = () => $header.classList.remove('translucent');
//     window.addEventListener('scroll', function() { 
//         $scrollPos = window.scrollY;
//         if ($scrollPos >= $headerHeight) { $addClassOnScroll(); }
//         else { $removeClassOnScroll(); }
//     });
// });
